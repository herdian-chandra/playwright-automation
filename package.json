{
  "name": "playwright-automation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test-all": "npx playwright test",
    "regression": "npx playwright test --grep @regression",
    "smoke": "npx playwright test --grep @regression --grep @smoke",
    "allure-report": "npx playwright test --grep @smoke --reporter=line,allure-playwright"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.7",
    "allure-playwright": "^3.3.2",
    "exceljs": "^4.4.0"
  }
}
